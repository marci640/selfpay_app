
<br> 
<div class="container">
<h3> How your bill compares to Medicare's rates: </h3>
<br>

<%= form_tag "/letters", method: :post do %>
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Code</th>
        <th>Hospital Charge</th>
        <th>Facility Rate</th>
        <th>Non-Facility Rate</th>
        <th>The Difference</th>
        <th>Add Results to Letter?</th>
      </tr>
    </thead>
    <tbody>
    <% @arrays.each_with_index do |array, index| %> 
      <% index = 1 %>
        <tr>
          <td><%= array[0].code %></td>
          <td><%= number_to_currency(array[1]) %></td>
          <td><%= number_to_currency(array[2].facility_fee)%></td>
          <td><%= number_to_currency(array[2].non_facility_fee)%></td>
          <td class="difference"><%= number_to_currency((array[1].to_i - array[2].facility_fee))%></td>
          <td><div class="form-check">
              <label class="form-check-label">
                <input type="checkbox" name="table_row<%=index%>" value="<%=array%>" class="form-check-input"> 
                Add to Letter
              </label>
            </div>
          </td>
        </tr>
    <%end%>
    </tbody>
  </table>

<hr> 
<br>
<!-- line break -->

<h3> Generate a Letter </h3>

<h5> To Generate a letter to <%=@bill.hospital%> for date of service: <%=@bill.dos%>, checkmark the data you'd like to include above and click the button below. </h5> 
<button class="btn btn-primary" type="submit" href="/letters">Generate Letter</button>
<%end%>


  
    
   
  



